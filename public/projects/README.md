Drop project screenshots here (PNG or WebP, ~1600×1000).

Reference them from `src/data/projects.ts` as `image: '/projects/your-file.png'`.
Any project without an `image` falls back to a generated gradient placeholder.
